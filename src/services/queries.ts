import { useQuery } from "react-query";

// export const useExample = (queryParams, options = {}) => {
//   const { data, isFetching, refetch } = useQuery(
//     [EXAMPLE_QUERY_KEY, queryParams],
//     serviceFn,
//     {
//       ...options,
//     }
//   );
//   return { data, isLoading: isFetching, refetch };
// };
